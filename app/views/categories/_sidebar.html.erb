<div class="sidebar_header"><h3>Categories</h3></div>

<div class="sidebar">
    
  <% @categories.each do |category| %>
    <div><%= link_to "#{category.name} (#{category.entries.size})", category_path(category) %></div>
	<% end %>

</div>
