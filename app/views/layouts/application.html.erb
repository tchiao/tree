<!DOCTYPE html>
<html>
<head>
  <title>History Tree</title>
  <link href='https://fonts.googleapis.com/css?family=Open+Sans:400,700,800,300' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  
</head>

<body>
  <div class="container">
  	<div class="header">
      <%= link_to(root_path) do %>
        <%= image_tag("tree_logo2.png", height: "50") %>
      <% end %>

      <div class="pull-right">
        <% if current_user %>
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a href="" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.email %>
                <span class="caret"></span>
              </a>

              <ul class="dropdown-menu" role="menu">
                <li><%= link_to "Edit Account", edit_user_registration_path %></li>
                <li class="divider"></li>
                <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
                <div class="chat-bubble-arrow-border"></div>
                <div class="chat-bubble-arrow"></div>
              </ul>
            </li>
          </ul>

        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: 'login-link' %> or <%= link_to "Sign Up", new_user_registration_path, class: 'login-link' %>
        <% end %>
      </div>
  	</div>

    <div id="flash-container">
      <% if flash[:notice] %>
        <div class="alert alert-notice"><%= flash[:notice] unless notice.blank? %></div>
      <% elsif flash[:error] %>
        <div class="alert alert-error"><%= flash[:error] unless error.blank? %></div>
      <% elsif flash[:alert] %>
        <div class="alert alert-alert"><%= flash[:alert] unless alert.blank? %></div>
      <% end %>
    </div>

    <br/>
    <%#= render :partial => '/categories/sidebar' unless @disable_nav %>
    
    <div><%= yield %></div>
</body>


</html>
