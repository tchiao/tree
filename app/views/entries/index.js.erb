// $('.entries_index').html("<%#=j render partial: 'entry', collection: @entries %>");

<%#= render partial: 'timeline' %>

$(document).ready(function() {
  $("#timeline-embed").html("")

  createStoryJS({
    type:       'timeline',
    width:      '100%',
    height:     '600',
    source:     "<%= '/entries.json?keyword_search=' + params[:keyword_search] %>",
    embed_id:   'timeline-embed'
  });
});